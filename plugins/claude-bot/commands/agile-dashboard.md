---
name: agile-dashboard
description: 生成并显示敏捷开发进度看板
version: 1.0.0
---

# Agile Dashboard - 进度看板

请执行以下步骤生成进度看板：

## 第一步：读取项目状态

```bash
# 获取当前迭代
if [ ! -f "projects/active/iteration.txt" ]; then
    echo "❌ 项目未初始化，请先运行 /agile-start"
    exit 1
fi

iteration=$(cat projects/active/iteration.txt)
status_file="projects/active/iterations/${iteration}/status.json"

if [ ! -f "$status_file" ]; then
    echo "❌ 状态文件不存在，请先运行 /agile-start"
    exit 1
fi

# 读取状态数据
total_tasks=$(jq '.progress.tasks_total' "$status_file")
completed_tasks=$(jq '.progress.tasks_completed' "$status_file")
in_progress_tasks=$(jq '.progress.tasks_in_progress' "$status_file")
pending_tasks=$(jq '.progress.tasks_pending' "$status_file")
completion_percentage=$(jq '.progress.completion_percentage' "$status_file")
```

## 第二步：生成进度报告

```bash
echo "╔════════════════════════════════════════════════════════════╗"
echo "║           📊 Agile Flow - 迭代 $iteration 进度看板           ║"
echo "╚════════════════════════════════════════════════════════════╝"
echo ""
echo "📈 总体进度"
echo "─────────────────────────────────────────────────────────────"
echo "  完成度: $completed_tasks/$total_tasks 任务 ($completion_percentage%)"
echo "  进行中: $in_progress_tasks 任务"
echo "  待处理: $pending_tasks 任务"
echo ""

# 显示当前任务
current_task=$(jq -r '.current_task // "无"' "$status_file")
if [ "$current_task" != "null" ] && [ -n "$current_task" ]; then
    echo "🎯 当前任务"
    echo "─────────────────────────────────────────────────────────────"
    echo "  $current_task"
    echo ""
fi

# 显示待办任务（前5个）
pending_list=$(jq -r '.pending_tasks[0:5][]' "$status_file")
if [ -n "$pending_list" ]; then
    echo "📋 待办任务 (前5个)"
    echo "─────────────────────────────────────────────────────────────"
    echo "$pending_list"
    echo ""
fi
```

## 第三步：显示缺陷统计

```bash
bug_count=$(jq '.bugs | length' "$status_file")
if [ "$bug_count" -gt 0 ]; then
    echo "🐛 缺陷统计"
    echo "─────────────────────────────────────────────────────────────"
    jq '.bugs' "$status_file"
    echo ""
fi
```

## 第四步：显示阻塞因素

```bash
blocker_count=$(jq '.blockers | length' "$status_file")
if [ "$blocker_count" -gt 0 ]; then
    echo "⚠️  阻塞因素"
    echo "─────────────────────────────────────────────────────────────"
    jq '.blockers' "$status_file"
    echo ""
fi
```

## 第五步：显示快捷操作

```bash
echo "⚡ 快捷操作"
echo "─────────────────────────────────────────────────────────────"
echo "  继续开发: 告诉我你想开发哪个功能"
echo "  查看详情: 输入任务ID查看详细信息"
echo "  暂停项目: /agile-pause"
echo "  生成报告: /agile-retrospective"
echo ""
```

## 输出示例

```
╔════════════════════════════════════════════════════════════╗
║           📊 Agile Flow - 迭代 1 进度看板                ║
╚════════════════════════════════════════════════════════════╝

📈 总体进度
─────────────────────────────────────────────────────────────
  完成度: 6/10 任务 (60%)
  进行中: 1 任务
  待处理: 3 任务

🎯 当前任务
─────────────────────────────────────────────────────────────
  TASK-007

📋 待办任务 (前5个)
─────────────────────────────────────────────────────────────
  TASK-008
  TASK-009
  TASK-010

⚡ 快捷操作
─────────────────────────────────────────────────────────────
  继续开发: 告诉我你想开发哪个功能
  查看详情: 输入任务ID查看详细信息
```

## 注意事项

1. **自动更新**：任务完成后通过 PostToolUse hook 自动更新
2. **手动刷新**：修改任务文件后可手动运行此命令
3. **数据来源**：从 `status.json` 读取最新状态
